{
  "rules": {
    "live_locations": {
      "$eventId": {
        ".read": "auth != null",
        "$userKey": {
          ".write": "auth != null"
        }
      }
    },
    "pings": {
      "$pingId": {
        ".write": "auth != null"
      }
    }
  }
}
